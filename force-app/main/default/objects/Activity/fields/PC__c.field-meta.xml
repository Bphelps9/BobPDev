<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>PC__c</fullName>
    <externalId>false</externalId>
    <formula>IF(
 NOT(ISBLANK(Account__c)), Account__r.PC__c,
IF(
 NOT(ISBLANK(Opportunity__c)), Opportunity__r.PC__c,
IF(
 NOT(ISBLANK(Contact__c)), Contact__r.PC__c,
 &apos;&apos;)))</formula>
    <label>PC</label>
    <required>false</required>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
